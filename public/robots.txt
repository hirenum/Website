# Robots.txt for Hirenum
# https://hirenum.com

# Allow all web crawlers to access all content
User-agent: *
Allow: /

# Sitemap location
Sitemap: https://hirenum.com/sitemap.xml

# Crawl-delay (optional, helps prevent server overload)
Crawl-delay: 1

# Disallow admin or private routes (add as needed)
# Disallow: /api/
# Disallow: /admin/
